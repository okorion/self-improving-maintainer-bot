GITHUB_TOKEN=
GITHUB_REPOSITORY=OWNER/REPO
ANALYZE_GITHUB_TOKEN=
PUBLISH_GITHUB_TOKEN=
BOT_GITHUB_TOKEN=

# Target repository managed by this bot. Leave blank to manage this repository itself.
# Example: TARGET_REPOSITORY=OWNER/TARGET_REPO
TARGET_REPOSITORY=
TARGET_DEFAULT_BRANCH=main
TARGET_WORKTREE=targets/active
TARGET_DOC_PATHS=README.md,docs
# Optional target-specific eval cases. Leave blank to use this bot's evals.
TARGET_EVALS_PATH=
# Optional target publish guard. Prefer profiles/*.json for multi-repo operation.
TARGET_ALLOWED_PATHS=README.md,docs/**,maintainer-bot/**
TARGET_DENY_PATHS=.github/workflows/**,CODEOWNERS,.env*,.npmrc,infra/**,terraform/**,k8s/**,migrations/**,**/auth/**,**/security/**,*.pem,*.key
TARGET_MAX_FILES=20
TARGET_MAX_LINES=500

# Optional. Set only when the Codex CLI is not on PATH.
CODEX_CLI=
CODEX_TIMEOUT_SECONDS=3600
